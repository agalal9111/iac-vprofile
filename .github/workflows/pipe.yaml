name: my infra pipeline

on:
    push:
        branches:
            - main
            - stage
    pull_request:
        branches:
            - manin    
jobs:
    build:
        name: terraform plan and validate
        runs-on: ubuntu-latest

        steps:
            - name: checkout
              uses: actions/checkout@v4

